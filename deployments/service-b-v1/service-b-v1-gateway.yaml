apiVersion: networking.istio.io/v1beta1
kind: Gateway
metadata:
  name: service-b-v1-gateway
  namespace: istio-meshlab
  labels:
    app: service-b-v1
spec:
  servers:
    - port:
        number: 443
        name: https
        protocol: HTTPS
      tls:
        mode: SIMPLE
        credentialName: service-b-v1-tls
      hosts:
        - "service-b-v1.local"
